# we should use this to set all the set-up files (This would be more official way to follow)
# image: registry.baidubce.com/paddlepaddle/paddle:3.0.0b1-gpu-cuda11.8-cudnn8.6-trt8.5

# paddlepaddle-gpu
# torch==2.0.0
# torch_scatter
# torch_sparse
# torch_geometric
# torch_harmonics==0.6.2

hydra-core
# lightning
# open3d==0.18.0   # requires torch 2.0.0 and CUDA=11.8
# tensorboard
h5py~=3.11.0
meshio
pyyaml
pynvml

# must add 'x = x.to(torch.float32)' to /neuralop/layers/spectral_convolution.py line433
neuraloperator==0.3.0
# tensorly
# tensorly-torch
# wandb 
ruamel.yaml
configmypy
opt-einsum
zarr
# torchtyping

numpy~=1.26.4
pandas~=2.2.1